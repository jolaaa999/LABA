export interface StoryMilestone {
  id: string
  index: string
  label: string
  title: string
  statement: string
  year?: string
  /** Development placeholder — replace with verified community history */
  placeholder?: boolean
}
